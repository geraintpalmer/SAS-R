"""
Class test - quesion 3
C1325939
"""

import random

class Coordinate():
    """
    A class of a point landing under the graph

    Attributes:
         x: the x coordinate of the point
         y: the y coordinate of the point
         undergraph: a boolean indicating whether or not the point is under the graph
    """
    def__init__(self, (1 - x ** 2):
        self.x = (.5 - random.random())
        self.y = (.5 - random.random())
        self.undergraph = integrate.(1 - (self.x) ** 2) #  This returns the boolean corresponding to whether or not the point is under the graph 
