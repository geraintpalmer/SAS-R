f = open("classtestdata.csv, 'r')  # Open a file in read mode
         f.close()  # Close file 
data = data.split('\n')  # Split on new lines 
data = [int(e) for e in data[:-1]]  # Convert to integer
         target = [names]
         print names
         
if data[index] == item: 
            found = True
        elif data[index] > item: 
            index = mean
        else: 
            index += 1
    if found: 
        return index 
    else: 
        return False
print [mean]            # the data will be added up and divided by 2
         
if data[index] == item:
         found = True
        elif data[index] > item:
             index = unique names
         else:
             index += 1
    if found:
         return index
    else:
         return False
print [uniquenames]     # the data will search for unique names and print this data
         



         

