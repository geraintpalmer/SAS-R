#q3

import random
import math

class drop():

    def __init__(self, r=1)
    self.x = (.5 - random.random()) * 2 * 0.5
    self.y = (.5 - random.random()) * 2 * 0.5
    self.undergraph = (self.y) ** 2 + (self.x) ** 2 <= (r)** 2


def approxpi(N=1000):
    numberofpointsundergraph = 0
    for i in range(N)
    drop = Drop()
    if drop.undergraph:
        numberofpointsundergraph += 1
    return 
