import random

class point():
    """
    A class for estimating the value of the area under a graph

    Attributes:
        x: the x coordinate of the point
    """
    def __init__(self, a=1)
    self.x = (1 - random.random()) * a
    self.areaundergraph = [(self.x) - (self.x) ** 2]
