textfile = open('classtestdata.csv', 'r') #We are reading the file into python, by locating it and then converting it in
data = textfile.read()
textfile.close()

data = data.split('\n') # Spliting on the new line on the command 

#data = [int(e) for e in data[:-1]] 

print data


"""

Every element of data we are converting the string into an interger.

We also have to remove the last line of the data in this as it has an empty line, and python can't convert that into an interger. 

"""

print len(data) #total number of names in list
