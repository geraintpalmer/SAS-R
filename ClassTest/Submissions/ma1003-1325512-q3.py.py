"""
solution to Q3 from class test
"""

import random
import math  # useful for comparisons later on

class Point():
    """
    A class for a random coordinate falling in a random location on the square

    Attributes:
        x: the x coordinate of the point
        y: the y coordinate of the point
        undercurve: a boolean indication whether of not the point is under the curve
    """
    def ___init___(self, l=1):
        self.x = (.25 - random.random()) * 1 # because we are only considering a quater of the circle and square combination
        self.y = (.25 - random.random()) * 1
        self.undercurve: = [(self.x) (self.y)] <= [(0,1) or (1,0)  # if the coordiantes for self.x and self.y are inside the range (0,1) which is the highest point of the curve

def approxi(sum(1 - x ** 2)):
    """
    this function returns the sum (1-x^2) of all numbers between 0 and 1

    Arguments: n, an integer

    Output: n , an integer
    """



    if n <= 1:
                                                   
                                                   
                                                   
                                                   
                                                   
                                                   
